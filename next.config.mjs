/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['encrypted-tbn3.gstatic.com', 'encrypted-tbn2.gstatic.com', 'encrypted-tbn0.gstatic.com']
    }
}

export default nextConfig
